procs:
  api:
    cwd: apps/api
    shell: npm run dev
  web:
    cwd: apps/web
    shell: npm run dev
  tunnel:
    shell: sh -c 'set -a; . apps/api/.env; set +a; exec cloudflared tunnel run --token "$TUNNEL_TOKEN"'
  keepawake:
    shell: caffeinate -is
