node_modules/
dist/
*.db
*.db-journal
.env
.DS_Store
*.tsbuildinfo
*.db-wal
*.db-shm
# live-executor secrets — a keypair must never enter this repo, even misplaced
keypair*.json
*.keypair.json
id.json
.million/

# monte-carlo output — regenerate with tools/monte-carlo.mjs, don't version the run
docs/monte-carlo.json
